<?xml version="1.0" encoding="utf-8"?>
<!-- The artwork is cut square to whatever height the launcher gives (SizeMode.Exact), so the
     height range here only bounds how big that square can get. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/wren_widget_description"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:minWidth="250dp"
    android:minHeight="64dp"
    android:maxResizeWidth="400dp"
    android:maxResizeHeight="88dp"
    android:targetCellWidth="4"
    android:targetCellHeight="1"
    android:resizeMode="horizontal"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen" />
