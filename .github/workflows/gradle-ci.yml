name: Gradle CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle
      - uses: gradle/actions/setup-gradle@v6
      - name: Build and test
        run: ./gradlew build test

  distro:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            format: Deb
            name: linux
          - os: windows-latest
            format: Msi
            name: windows
          - os: macos-latest
            format: Dmg
            name: macos
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle
      - uses: gradle/actions/setup-gradle@v6
      - name: Build ${{ matrix.name }} distributable
        run: ./gradlew package${{ matrix.format }}
      - name: Rename artifact
        run: |
          cd build/compose/binaries/main
          case "${{ matrix.format }}" in
            Deb) ;; # wren_*.deb is already correct Debian convention
            Msi) mv wren-*.msi Wren-*.msi ;;
            Dmg) mv wren-*.dmg Wren-*.dmg ;;
          esac
      - uses: actions/upload-artifact@v6
        with:
          name: wren-${{ matrix.name }}
          path: |
            build/compose/binaries/main/Wren-*,build/compose/binaries/main/wren_*
